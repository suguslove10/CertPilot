http:
  routers:
    dashboard:
      rule: "Host(`traefik.certpilot.local`) || Host(`traefik-dashboard.${DOMAIN:-certpilot.local}`)"
      service: api@internal
      entryPoints:
        - dashboard
      middlewares:
        - auth
        - secureHeaders
  
  middlewares:
    auth:
      basicAuth:
        # Default credentials: admin/certpilot (please change in production)
        # Generated with htpasswd -nb admin certpilot
        users:
          - "admin:$apr1$ruca84Hq$mbjdMZBAG.KWn7vfN/SNK/" 